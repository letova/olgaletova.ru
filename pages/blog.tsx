import Head from 'next/head';

import Layout from '../components/layout';

const Blog = () => {
  return (
    <Layout>
      <Head>
        <title>Blog</title>
      </Head>
      <div>Blog</div>
    </Layout>
  );
};

export default Blog;
